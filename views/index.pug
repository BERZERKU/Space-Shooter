extends default

block content
  div.container
    each enemy in enemiesCategory
      div.person
        div.person-image(style=`background: url('/img/enemy.png') top center
        no-repeat; background-size: cover;`)
        h2.enemy-name
          | #{enemy.name}
        a(href=`/enemy?id=${enemy.id}`)
          | View Enemy